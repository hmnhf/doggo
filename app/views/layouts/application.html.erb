<!DOCTYPE html>
<html>
  <head>
    <title>Dog Explorer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('favicon.png') %>
    <%= combobox_style_tag %>
  </head>

  <body class="flex flex-col min-h-screen px-20 py-5">
    <%= render partial: "error_message" %>

    <div class="pb-20">
      <div class="text-3xl font-bold text-center sm:text-left">
        Dog Explorer
      </div>
    </div>

    <main class="container mx-auto flex flex-grow">
      <%= yield %>
    </main>

    <footer class="container mx-auto text-center text-slate-700 text-sm">
      <hr class="border-slate-300 mt-8 mb-4" />
      <div class="flex flex-wrap justify-center items-center gap-1">
        <div>Made with Ruby on Rails and Tailwind CSS.</div>
        <a href="https://github.com/hmnhf/doggo" target="_blank">
          View the code on <span class="underline">Github</span>.
        </a>
      </div>
    </footer>
  </body>
</html>
